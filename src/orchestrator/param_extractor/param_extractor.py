#we will write the parameter extraction logic in this file. This will be called from the orchestrator after we have the relevant chunks for each brand section. We will pass the brand section and the parameter extraction rules to the param extractor and it will return the extracted parameters for that brand section. The heading in the rule content is the parameter for which we will extract the value.